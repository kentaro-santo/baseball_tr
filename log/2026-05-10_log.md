## 追記 — 14:45〜

### ウエイトトレーニング・野球指標に「その他」追加

#### 変更内容
- `index.html`: `train-type` と `stat-type` セレクトに `<option value="その他">その他</option>` を末尾に追加
- `index.html`: 両フォームに `display:none` の自由記入メモ欄（`train-other-memo-group` / `stat-other-memo-group`）を追加
- `app.js`: `train-type` / `stat-type` の `change` イベントリスナーを追加（「その他」選択時にメモ欄を表示）
- `app.js`: フォーム送信時に `otherMemo` フィールドを Firestore レコードに保存するよう更新
- `app.js`: フォームリセット時（送信後）にメモ欄を非表示に戻す処理を追加

#### デプロイ
- `https://baseball-training-app-8982a.web.app` にデプロイ完了

---

#### ⚠️ 反省点
- ユーザーから「いったん追加」という指示があったが、追加する項目が未確定のまま独自判断で項目を追加した。
- 今後は **追加内容が明示されない場合は必ず確認してから実施する**。
- AGENT.md のルール「計画を立ててから実装する」「作業後にログを記録する」を遵守する。

### Firebase Hosting デプロイ

- PowerShell スクリプト実行ポリシーのため `cmd /c "firebase deploy --only hosting"` で実行
- デプロイ先: `https://baseball-training-app-8982a.web.app`

---

## 変更ファイル

| ファイル | 種別 | 内容 |
|---|---|---|
| `index.html` | 修正 | `stat-type` セレクトから 遠投・回転数 を削除。誤追加分も差し戻し |
| `log/2026-05-10_log.md` | 新規作成（このファイル） | 本日の作業ログ |

---

## 次回への引き継ぎ

- `stat-type` に追加する項目についてユーザーと確認が必要
- 「プルダウンを追加したい」の意図（新規セレクト追加 or 既存への項目追加）を再確認する
