# 2026-06-01 作業ログ

## AGENT.md ログ運用ルール追記

- `AGENT.md` の「開発ログは `log/` フォルダに保存する」セクションへ、今後のAI作業では完了後に必ず当日のログへ作業内容・変更ファイル・確認結果を追記する旨を明記した。
- 変更ファイル: `AGENT.md`, `log/2026-06-01_log.md`
- 確認結果: 既存のログ保存ルールと重複しすぎない位置に追記されていることを確認した。

## Firebase Hosting デプロイ

- `firebase.cmd deploy --only hosting` を実行し、Firebase Hosting へデプロイした。
- 初回デプロイ時に `firebase.json` の `hosting.public` が `.` だったため、作業ログ・Markdown・重複フォルダ・Git管理情報などが公開対象に含まれることを確認した。
- `firebase.json` の `hosting.ignore` に Markdown、ログ、計画、Git管理情報、重複フォルダ、Nodeインストーラ類を除外する設定を追加した。
- 修正後に再度 `firebase.cmd deploy --only hosting` を実行し、公開対象が 7 files まで絞られた状態でデプロイ完了した。
- 変更ファイル: `firebase.json`, `log/2026-06-01_log.md`
- デプロイ先: `https://baseball-training-app-8982a.web.app`

## スマホメニューボタン位置変更

- `index.html` の `id="mobile-menu-btn"` をページタイトル左側からヘッダー右側の `.header-actions` 内へ移動した。
- JSは `id="mobile-menu-btn"` を参照しているため、IDは変更せず、既存のメニュー開閉処理をそのまま使う構成にした。
- 変更ファイル: `index.html`, `log/2026-06-01_log.md`
- 確認結果: `mobile-menu-btn` が `index.html` 内に1つだけ存在し、右側アクション領域へ移動していることを確認した。
