rules_version = '2';

// TEMPORARY BOOTSTRAP RULES
// These rules allow any authenticated user to write.
// Replace with proper rules after first master is created.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
